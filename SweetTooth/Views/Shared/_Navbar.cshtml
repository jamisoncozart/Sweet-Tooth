<nav class="navbar navbar-expand-lg navbar-light bg-light rounded">
  <div class="container">
    <a class="navbar-brand" href="#">Sweet Tooth</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExample09" aria-controls="navbarsExample09" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarsExample09">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <a class="nav-link home" href="/">Home <span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/Sweets">Sweets</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/Flavors">Flavors</a>
        </li>
        @if(!User.Identity.IsAuthenticated)
        {
          <li class="nav-item ml-auto">
            <a class="nav-link" href="/Account/Register">Sign Up</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/Account/Login">Log in</a>
          </li>
        }
      </ul>
      @if(User.Identity.IsAuthenticated)
      {
        <ul class="nav navbar-nav ml-auto">
          <li class="nav-item">
            <li class="nav-item">
              <p class="btn btn-link content">@User.Identity.Name</p>
            </li>
            @using (Html.BeginForm("LogOff", "Account"))
            {
              <input type="submit" value="Log Out" class= "btn btn-link link">
            }
          </li>
        </ul>
      }
    </div>
  </nav>
  <nav class="navbar navbar-light bg-light">
    <div class="container">
      <form class="form-inline ml-auto">
        @using (Html.BeginForm("Search", "Sweets", FormMethod.Get)) 
          {
            @Html.TextBox("Search", null, new {@class="form-control mr-sm-2"})
            <input type="submit" value="Search" class="btn btn-outline-dark my-2 my-sm-0">
          }
      </form>
    </div>
  </div>
</nav>